#ifndef NAVIGATION_H
#define NAVIGATION_H
/**
 * Function to handle menu navigation and user input.
 * It takes a pointer to the selected index and updates it based on the user input.
 * The function will continuously process key presses and handle the menu actions.
 */
void handle_navigation(int *selected_index);
#endif
